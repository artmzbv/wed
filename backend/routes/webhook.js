// const express = require('express');
// const router = express.Router();
// const { handleCalendarWebhook, renewWebhooks } = require('../controllers/webhook');

// // Route for handling Google Calendar webhooks (this is the URL where Google Calendar will send events)
// router.post('/calendar', handleCalendarWebhook);

// // Route for renewing the Google Calendar webhooks
// router.post('/calendar/renew', renewWebhooks);

// module.exports = router;
